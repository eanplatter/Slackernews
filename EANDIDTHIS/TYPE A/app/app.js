// This is where we will keep our angular module. It is kind of like the INDEX.html of your angular App.

// This is where we will hold all of the routes for the app. It is also where we will inject all of our dependencies. 

// Remember? It looks something like this: var app = angular.module('slackerNews', ['dependency', 'dependency', 'dependency', ])